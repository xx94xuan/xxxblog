<%= month_calendar(events: @events) do |date, events| %>
  <%= date.day %>

  <% events.each do |event| %>
    <%= render partial: 'events/calendars/event_show', locals: {event: event, today: date.day} %>
  <% end %>
<% end %>
